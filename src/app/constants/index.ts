export enum LocalStorageItems {
  Subjects = 'Subjects',
  Schedule = 'Schedule'
}

export enum FieldTypes {
  Text = 'text',
  Number = 'number',
  File = 'file'
}

export enum Keyboard {
  Enter = 'Enter',
  Space = 'Space',
  Esc = 'Escape',
  ArrowUp = 'ArrowUp',
  ArrowDown = 'ArrowDown',
  ArrowLeft = 'ArrowLeft',
  ArrowRight = 'ArrowRight'
}
